var laterTestDelay = 50;

beforeEach(function() {

/*  this.addMatchers({
    toFailInstallation: function() {
      var callback = jasmine.createSpy();
      runs(function() {
        AppClient.install({manifest:this.actual, callback:callback});
      });
      waits(laterTestDelay);
      runs(function() {
        expect(callback).not.toHaveBeenCalled();
        return 
      });
    }
  })*/

});
